(define size 2)

(define (print x)
  (display x)
  (newline))

(print size)

(print (* 5 size))


(define pi 3.14159)
(define radius 10)
(print (* pi (* radius radius)))
(define circumference (* 2 pi radius))
(print circumference)
